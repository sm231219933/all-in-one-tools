export const calculate=(type:string,a:number,b:number,c:number,d:number)=>{switch(type){case'percentage':return `${a}% of ${b} = ${(a*b/100).toFixed(2)}`;case'gst':return `GST = ${(a*b/100).toFixed(2)} | Total = ${(a*(1+b/100)).toFixed(2)}`;case'discount':return `Discount = ${(a*b/100).toFixed(2)} | Final price = ${(a*(1-b/100)).toFixed(2)}`;case'simple-interest':return `Interest = ${(a*b*c/100).toFixed(2)} | Amount = ${(a+a*b*c/100).toFixed(2)}`;case'compound-interest':{const x=a*Math.pow(1+b/100,c);return `Interest = ${(x-a).toFixed(2)} | Amount = ${x.toFixed(2)}`};case'profit-loss':return b>=a?`Profit = ${(b-a).toFixed(2)} | Profit % = ${((b-a)/a*100).toFixed(2)}%`:`Loss = ${(a-b).toFixed(2)} | Loss % = ${((a-b)/a*100).toFixed(2)}%`;case'average':return `Average = ${((a+b+c+d)/4).toFixed(2)}`;case'bmi':{const x=b/100;return `BMI = ${(a/(x*x)).toFixed(2)}`};case'emi':{const r=b/1200,n=c*12,m=r?a*r*Math.pow(1+r,n)/(Math.pow(1+r,n)-1):a/n;return `Monthly EMI = ${m.toFixed(2)} | Total = ${(m*n).toFixed(2)} | Interest = ${(m*n-a).toFixed(2)}`};default:return ''}}

export const convert=(type:string,v:number)=>{const m:any={length:[1,.001,3.280839895,39.37007874],weight:[1,.001,2.20462262,35.2739619],temperature:[]};if(type==='temperature')return `${v} °C = ${(v*9/5+32).toFixed(2)} °F = ${(v+273.15).toFixed(2)} K`;if(type==='length')return `${v} m = ${(v*m.length[1]).toFixed(4)} km = ${(v*m.length[2]).toFixed(4)} ft = ${(v*m.length[3]).toFixed(4)} in`;if(type==='weight')return `${v} kg = ${(v*m.weight[1]).toFixed(4)} g = ${(v*m.weight[2]).toFixed(4)} lb = ${(v*m.weight[3]).toFixed(4)} oz`;if(type==='data')return `${v} MB = ${(v/1024).toFixed(4)} GB = ${(v*1024).toFixed(0)} KB`;return ''}

export const textTool=(type:string,s:string)=>{switch(type){case'word-counter':return `Words: ${s.trim()?s.trim().split(/\s+/).length:0}\nCharacters: ${s.length}\nSentences: ${(s.match(/[.!?]+/g)||[]).length}\nParagraphs: ${s.split(/\n\s*\n/).filter(Boolean).length}`;case'case-converter':return `UPPERCASE:\n${s.toUpperCase()}\n\nlowercase:\n${s.toLowerCase()}\n\nTitle Case:\n${s.toLowerCase().replace(/\b\w/g,x=>x.toUpperCase())}`;case'text-cleaner':return s.replace(/[ \t]+/g,' ').replace(/\n{3,}/g,'\n\n').trim();case'duplicate-lines':return [...new Set(s.split(/\r?\n/))].join('\n');case'text-sorter':return s.split(/\r?\n/).sort((a,b)=>a.localeCompare(b)).join('\n');case'reverse-text':return s.split('').reverse().join('');case'sentence-counter':return `Sentences: ${(s.match(/[^.!?]+[.!?]+/g)||[]).length}`;default:return s}}

export const jsonFormat=(s:string,min=false)=>{const x=JSON.parse(s);return JSON.stringify(x,null,min?0:2)}
